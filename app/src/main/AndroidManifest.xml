<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.afinal">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Post_Add_Home_Dash"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".CallbackMap_Dash"
            android:label="@string/title_activity_callback_map__dash" />
        <activity
            android:name=".Set_Current_Location"
            android:label="@string/title_activity_set__current__location" />
        <activity android:name=".AddHome" />
        <activity
            android:name=".LoginUserDashboard"
            android:label="@string/Dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SelectPostType"
            android:label="@string/SelectAddType" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".SearchActivity"
            android:label="@string/searchAdd" />
        <activity
            android:name=".SelectActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Profile_Edite_Dash"
            android:label="@string/EditProfiles" />
        <activity android:name=".Testin_Dash" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".Map_Dash"
            android:label="@string/title_activity_map__dash" />
        <activity android:name=".Post_Dash" />
        <activity
            android:name=".Profile_Dash"
            android:label="@string/Profilr" />
        <activity android:name=".Registration_Dash" />
        <activity android:name=".Forget_Dash" />
        <activity android:name=".Login_Dash" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>